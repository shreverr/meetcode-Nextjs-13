import type { Metadata } from "next" 

export const metadata: Metadata = {
  title: 'Meetcode',
  description: 'Coding classes evolved',
}

export default function Home() {
  return (
   <div>meeting page works</div>
  ) 
}
